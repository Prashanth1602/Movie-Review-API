services:
  web:
    command: >
      sh -c "python -m app.pre_start &&
             alembic upgrade head &&
             gunicorn app.main:app
             -k uvicorn.workers.UvicornWorker
             --bind 0.0.0.0:8000
             --workers 3"
    ports:
      - "8000:8000"